# masterLib et masterstruct sont développé par Poparnassus30

# masterlib, est une concentration d'outils sous forme de "lib" packagé pour facilité le code de mes application avec mes techniques, masterstrcut utilise des libs "masterlib" et des services "masterservices"

# masterstruct est une structure de programme qui encapsule votre application métier, permet en réalité d'avoir un chassis comment à tout programme # pour ne pas à chaque fois, recommencer la programmation de la structure de votre programme. Factorisation optimisation.


# """
# on active les services via le fichier "conf_service.ini" nmap_start = True or False, 
# le kernel voit les mise à jour, il le consigne dans le registre "changement d'état", effectue des actions en consequence (methode)
# le kernel_ihm lit les données du registre et les affiches, c'est un ihm system.
# le service ihm_graph démarre à l'activation "True" du parametre

# service en dev
# service_nmap = True
[service.wrapper]
service_nmap = True

[service.interne]



"""